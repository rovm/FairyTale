<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="donghwa.dao.BoardDao">

  <resultMap type="board" id="boardMap">
    <id column="bwno" property="no"/>
    <id column="mno" property="mno"/>
    <id column="cno" property="cno"/>
    <result column="name" property="name"/>
    <result column="bw_titl" property="bw_titl"/>
    <result column="bw_con" property="bw_con"/>
    <result column="bw_wdt" property="bw_wdt"/>
    <result column="bw_edt" property="bw_edt"/>
    <result column="bw_hits" property="bw_hits"/>
    <result column="bw_div" property="bw_div"/>
    <result column="c_con" property="c_con"/>
    <result column="c_wdt" property="c_wdt"/>
    <result column="filepath" property="file_Path"/>
    <result column="filename" property="file_Name"/>
  </resultMap>

  
  
  <insert id="insert" parameterType="board" useGeneratedKeys="true" keyColumn="bwno" keyProperty="no">
    insert into boardwrite(mno, bw_titl, bw_con, bw_wdt, bw_div) 
    values(#{mno}, #{bw_titl}, #{bw_con}, curdate(), 'board')
  </insert>
  <insert id="insert2" parameterType="board">
    insert into boardfile(bwno, filepath, filename) 
    values(#{no}, #{filePath}, #{fileName})
  </insert>
  
  <insert id="contextInsert">
    insert into boardwrite(mno, bw_titl, bw_con, bw_wdt, bw_div) 
    values(#{mno}, #{bw_titl}, #{bw_con}, curdate(), 'board')
  </insert>
  
  <insert id="commentInsert" parameterType="board">
    insert into comment(bwno, mno, c_con, c_wdt) 
    values(#{no}, #{mno}, #{c_con}, curdate())
  </insert>
  
   <select id="selectList" resultMap="boardMap" parameterType="map">
    select bw.bwno, bw.bw_titl, bw.bw_con, bw.bw_wdt, m.name
    from boardwrite bw inner join member m on bw.mno = m.mno
    where bw_div = 'board'
    order by bwno desc
    limit #{startIndex}, #{pageSize}
  </select>
  
  <select id="comSelectList" resultMap="boardMap" parameterType="map">
    select bw.bwno,m.name,c.mno,c.cno, c.c_con, c.c_wdt
    from boardwrite bw inner join comment c on bw.bwno = c.bwno
    join member m on c.mno = m.mno
    where bw.bwno = #{bwnoNo}
    limit #{startIndex}, #{pageSize}
  </select>
  
   <select id="selectListByTitle" resultMap="boardMap" parameterType="map">
    select bw.bwno, bw.bw_titl, bw.bw_con, bw.bw_wdt, m.name
    from boardwrite bw inner join member m on bw.mno = m.mno
    where bw_titl like '%${keyword}%' and bw_div = 'board'
    ORDER BY bwno DESC
   </select>
   
  <select id="countAll" resultType="int">
    select count(*)
    from boardwrite
  </select>
  
  <select id="comCountAll" resultType="int">
    select count(*)
    from boardwrite bw inner join comment c on bw.bwno = c.bwno
    join member m on c.mno = m.mno
    where bw.bwno = #{no}
  </select>
  
  <select id="suchCountAll" resultType="int" parameterType="map">
    select count(*)
    from boardwrite bw inner join member m on bw.mno = m.mno
    where bw_titl like '%${keyword}%' 
  </select>

  <select id="selectOne" resultMap="boardMap" parameterType="int">
    select  m.name, bw.bw_titl, bw.bw_con, bw.bw_wdt, bw.bwno
    from boardwrite bw inner join member m on bw.mno = m.mno
    where bwno= #{no}
  </select>
  
  <delete id="delete" parameterType="int">
    delete from boardwrite where bwno=#{no}
  </delete>
  
  <delete id="comDelete" parameterType="int">
    delete from comment 
    where cno = #{no}
  </delete>
  
  <update id="update" parameterType="board">
    update boardwrite set 
      bw_titl=#{bw_titl}, 
      bw_con=#{bw_con}, 
      bw_edt=curdate()
      where bwno = #{no}
  </update>
</mapper>









